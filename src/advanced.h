#ifndef _H_ADVANCED_H
#define _H_ADVANCED_H

void advanced_recovery (
			dev_ctrl_t* dev, 
			const char* name, 
			const char* skip, //converted to number
			const char* fill, //converted to number
			io_ctrl_t* io_ctrl );

#endif
